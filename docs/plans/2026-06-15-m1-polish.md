# M1 Polish Plan

## Decisions Log
- [2026-06-15] AGREED: Polish M1 to "usable & documented" — README + runnable/guarded examples + subprocess CLI test coverage — with no changes to language semantics.
- [2026-06-15] AGREED: `examples/` holds only *runnable* programs; lexer fixture coupling moved off `examples/hello.phg` onto `tests/fixtures/sample.phg`.
- [2026-06-15] AGREED: A directory-sweep test (`tests/examples.rs`) runs every `examples/*.phg` and asserts exit 0 — structurally prevents broken examples (TDD: red on the broken hello.phg, green after fix).

## Formal Plan
1. `tests/examples.rs` (create, TDD) — sweep `examples/*.phg`, assert each runs at exit 0; assert ≥1 example found. Locate via `CARGO_MANIFEST_DIR`, run via `CARGO_BIN_EXE_phorge`.
2. `examples/hello.phg` (rewrite) — broken fragment → minimal runnable hello-world.
3. `tests/lexer_integration.rs` (modify) — repoint fixture from `examples/hello.phg` to `tests/fixtures/sample.phg` (which contains `function`/`=>`/`match`).
4. `examples/fib.phg` (create) — recursion showcase.
5. `tests/cli.rs` (modify) — subprocess tests: `parse` (exit 0), `lex` (exit 0), ill-typed `transpile` → exit 1, runtime-error `run` → exit 1 (uniquely-named temp files).
6. `README.md` (create) — overview, build, 5 subcommands, language summary, transpiler note, M1 limitations; every snippet verified against the binary.

## Verification
`cargo test` all green (incl. examples sweep + CLI subprocess), `cargo clippy --all-targets` exit 0, README snippets hand-run.

## Status
Done — M1 polish complete.

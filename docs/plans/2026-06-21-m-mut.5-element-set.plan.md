# M-mut.5 — value-type element set (`xs[i]=e`, `m[k]=e`)

> Slice 5 of the mutation milestone (`docs/specs/2026-06-21-mutation-milestone-design.md` §4) — the
> first *interior* mutation, still **GC-free** (value types are acyclic). Autonomy: directive `f5f2912`.

**STATUS: COMPLETE.** Byte-identical run≡runvm≡real PHP 8.6; 645 tests, clippy+fmt clean.

**What landed:**
- One new `Op::SetIndex` (the three coupled matches: `vm::exec_op`, `chunk::validate` no-index arm,
  `compiler::stack_effect` = −2). Polymorphic like `Op::Index`: `List` → bounds-checked
  `value::list_set`, `Map` → update-or-append `value::map_set`. **COW via `Rc::make_mut`** — clones
  only if another binding shares the `Rc` (value semantics); acyclic ⇒ no GC.
- `Stmt::Assign` target extended from `Ident` to also accept `Expr::Index` (the parser already
  produced it; M-mut.2 compound `xs[i] += e` rides the same desugar). Container restricted to a
  **mutable local** `List<T>`/`Map<K,V>`; nested places (`a[i][j]`, `this.f[i]`) → `E-ASSIGN-TARGET`
  (deferred). Checker split into `check_local_reassign` + `check_index_assign`.
- Backends: interpreter COW-set + write-back (eval order index-then-value, matching the VM);
  compiler `GetLocal; index; value; SetIndex; SetLocal`; transpiler `$xs[$i] = $e`.
- `value::list_set`/`map_set` kernels shared by both backends (F3).
- Tests: 5 checker, differential `mutation_element_set_agrees` (incl. the F13 COW two-binding
  catcher for both List and Map) + `mutation_element_set_oob_faults_agree` (run≡runvm OOB fault).
  Guide `examples/guide/element-set.phg`.

**Known divergence (KNOWN_ISSUES):** an out-of-range list **set** faults in Phorge
(`list index out of range`, `FaultKind::IndexOob`) but PHP would *extend* the array — so the OOB
case is `agree_err` (run≡runvm only), **not** PHP-gated; examples set only in-bounds.

**Deferred:** in-place slot-direct mutation (avoid the `GetLocal`-induced clone when the container is
unshared) — a bench-gated perf optimization; correctness/COW semantics are already right. Nested
place-stores + `this.field[i]=e` land with M-mut.6 (instance fields).

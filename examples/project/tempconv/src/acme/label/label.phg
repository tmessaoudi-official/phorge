package acme.label;

// A second library package. `tag` formats a labelled integer; imported in main as `fmt` (aliased)
// and called `fmt.tag(...)`.
function tag(string name, int value) -> string {
    return "{name} = {value}F";
}
